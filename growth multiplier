# Create a variable savings
savings = 100

# Create a variable growth_multiplier
grownth_multiplier = 1.1

# Calculate result
result = savings * grownth_multiplier ** 7

# Print out result
print(result)
